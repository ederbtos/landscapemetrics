{
  "name": "landscapemetrics-frontend",
  "private": true,
  "version": "1.0.0",
  "description": "Fonte TypeScript do frontend estático (compila para static/app.js) — ver frontend-src/.",
  "scripts": {
    "build": "tsc && tsc -p tsconfig.atlas.json",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.14",
    "playwright": "^1.62.1",
    "typescript": "^5.7.3"
  }
}
